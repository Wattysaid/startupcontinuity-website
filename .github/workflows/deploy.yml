name: Build & Deploy to Hostinger (FTP, pnpm)

on:
  push:
    branches: [ "main" ]   # change if you deploy from a different branch
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'         # cache keyed from pnpm-lock.yaml

      # Ensure pnpm is available on PATH
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9            # or omit to use latest v9
          run_install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build (Astro)
        run: pnpm build

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ${{ secrets.FTP_PROTOCOL }}   # use ftps if available
          # port: ${{ secrets.FTP_PORT }}         # only if Hostinger told you a non-default port
          local-dir: dist/
          server-dir: ${{ secrets.FTP_REMOTE_DIR }}/
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/node_modules/**
            **/.DS_Store
